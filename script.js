const byteSize = (str) => {
  // write your code here
	    const size = str.size;
		return size;
	}   // return size;
};

// Do not change the code below
const str = prompt("Enter some string.");
alert(byteSize(str));
